# 梅花易数世界杯预测器

基于梅花易数起卦、世界杯对阵信息和 DeepSeek 大模型的足球预测演示项目。

## Run Locally

**Prerequisites:** Node.js


1. Install dependencies:
   `npm install`
2. Configure `DEEPSEEK_API_KEY` in your local environment or `.env`.
   Do not commit real API keys.
3. Run the app:
   `npm run dev`
